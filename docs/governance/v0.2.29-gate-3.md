# v0.2.29 Gate 3 — Test, Lint, Build/Error Sweep

**Parent:** Gate 2 (completed)  
**Next:** Gate 4 (PR, tag, publish and verify)  
**Tracked in:** GitHub Issue [#41](https://github.com/tuyoisaza/keledon/issues/41)

---

## ✅ Gate 2 Completed

| Item | Status |
|------|--------|
| Cleaned untracked files (backup, broken ci.yml, placeholder monitoring) | ✅ |
| Added 25 regression tests in `cloud/test/regression/` | ✅ |
| Added `test:regression` script to `cloud/package.json` | ✅ |
| Added `cloud/jest.config.js` (ts-jest ESM preset) | ✅ |
| Pushed to `main` (commit `826ece2`) | ✅ |

**Regression test suites:**
- `01-deep-link-diagnostics.test.js` — URL parsing, HMAC signature, expiry, fallback (7 tests)
- `02-auth-boundary.test.js` — RBAC authorization matrix (7 tests)
- `03-release-url-contract.test.js` — `/latest/` URL stability, env override (5 tests)
- `04-recovery-standby.test.js` — standby step presence, active vendor filtering (6 tests)

**Result:** `25 passed, 0 failed`

---

## 🔧 Gate 3 Checklist

### 1. Fix Jest TypeScript Runner (BLOCKER)

**Problem:** `jest` and `ts-jest` are listed in `package.json` / `package-lock.json` but are **not installed** in `node_modules` after `npm ci`. Running `npm test` fails with `jest: not found`. Running `npx jest` fails because Babel is used instead of `ts-jest` and TypeScript files cannot be parsed.

**Root cause (TBD):**
- Possible `package-lock.json` corruption or hoisting issue
- `jest` v30 + `ts-jest` v29 peer-dependency mismatch

**Actions:**
- [ ] Diagnose why `npm ci` skips `jest`/`ts-jest` installation
- [ ] Fix by aligning `ts-jest` to a version compatible with Jest 30, or downgrade Jest to 29
- [ ] Verify `node_modules/.bin/jest` exists after `npm ci`
- [ ] Verify `rbac.service.spec.ts` passes: `npx jest src/rbac/rbac.service.spec.ts --runInBand`
- [ ] Verify `e2e-websocket.test.ts` passes (if applicable)

### 2. Run All Tests
- [ ] `cd cloud && npm test` (must pass after Jest fix)
- [ ] `cd cloud && npm run test:regression` (already passing — keep green)
- [ ] `cd browser && npm run build` (TypeScript compile)
- [ ] `cd landing && npm run build` (Vite production build)
- [ ] `cd landing && npm run typecheck` (`tsc --noEmit`)

### 3. Lint Sweep
- [ ] `cd cloud && npm run lint`
- [ ] `cd browser && npm run lint` (if available)
- [ ] `cd landing && npm run lint`

### 4. Version Alignment Verification
- [ ] `browser/package.json` version matches target
- [ ] `cloud/package.json` version matches target
- [ ] `landing/package.json` version matches target
- [ ] `landing/src/pages/LaunchKeledonPage.tsx` download URL uses `latest` (not pinned)
- [ ] `cloud/src/health/health.service.ts` default URL uses `latest`

### 5. CI / Release Workflow Sanity
- [ ] `.github/workflows/release.yml` syntax is valid (`gh workflow view Release`)
- [ ] No broken `.github/workflows/ci.yml` in repo
- [ ] `railway.json` still valid for `CI_PROOF` tier

### 6. Error / Warning Sweep
- [ ] No `console.error` spam in cloud boot path
- [ ] No unhandled promise rejections in browser deep-link handler
- [ ] `KELEDON_RESET_DB` and `KELEDON_RESET_QDRANT` are `false` in prod
- [ ] `.env.production` does not contain secrets

---

## 📋 Gate 4 Handoff Criteria

Gate 3 must produce a clean `main` branch with:
1. All tests passing (Jest unit + regression + build/typecheck)
2. All lint clean
3. No console errors on boot
4. Version aligned across all packages
5. A green CI check on the last commit

Then Gate 4 can proceed:
- Create Git tag `v0.2.29`
- Trigger release workflow (Browser NSIS installer + GitHub Release)
- Verify assets and checksums
- Deploy cloud to Railway (staging → prod)
- Smoke-test landing download link

---

## 🏷️ Labels

`release-gate`, `v0.2.29`, `testing`, `ci-cd`
