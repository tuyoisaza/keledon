// KELEDON FLOW REGISTRY V1
// In V1, flows are cloud-authoritative and must be fetched at runtime.
// This registry is intentionally empty - do not add demo/mock flows.

export const FlowRegistry = {
    // Flows are fetched from cloud at runtime via WebSocket commands.
    // No local flow definitions should exist in the extension.
};
