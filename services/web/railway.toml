[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"
  watchPatterns = ["services/web/**"]

[deploy]
  numReplicas = 1
  healthcheckPath = "/"
  healthcheckTimeout = 120
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
